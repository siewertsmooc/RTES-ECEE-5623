The stripetest should XOR encode the file into chunks and the read it back, recovering data
from any lost chunk if necessary, but only 1 in the worst case.

This now needs updates from the second activity section.

This code is used in CSCI 430 as a working design and proof-of-concept example.
